#include <iostream>
using namespace std ;

int main(void) {
    int sum, value ;
    cout << "Give a value !" ;
    cin >> value ;
    sum = 0 ;
    while (value != 0) {
        sum = value % 10 + sum ;
        value = value / 10 ;
    }
    cout << sum ;
    return 0 ;
}
