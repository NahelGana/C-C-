#include <iostream>
using namespace std ;

int main(void) {
    int i, n, sum ;
    cout << "Give a value !" ;
    cin >> n ;
    sum = 0;
    for (i = 1 ; i <= 2 * n - 1 ; i = i + 2) {
        sum = sum + i ;
    }
    cout << sum ;
    return 0 ;
}
