#include <iostream>
using namespace std ;

int input_value(void) {
    int n ;
    do {
        cout << "Give an integer between 0 and 20." ;
        cin >> n ;
    } while ((n < 0) || (n > 20)) ;
    return n ;
}
int main(void) {
    cout << input_value() ;
    return 0 ;
}
